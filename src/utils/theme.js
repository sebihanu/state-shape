import { createMuiTheme } from '@material-ui/core';

//https://material-ui-next.com/customization/themes/#theme-configuration-variables

export const theme = createMuiTheme({
    typography: {
        useNextVariants: true,
    }
});